



if [ -z "$1" ]; then
    echo "There needs to be a bucket name eg. ./bucket my-bucket-name"
fi

bucket_name="$1"

aws s3api create-bucket \
--bucket $bucket_name \
--profile myprofile \
--query Location \
--output text
